public class ReverseString {
    public static void main(String[] args) {

        String str= "This is Ramya";
        String reverseStr = "";
         Char ch;

        for (int i = 0; i<str.length();  i++) {
         ch = str.charAt(i);
            reverseStr = ch + reverseStr;
        }

        System.out.println("Reversed String is : " + reversed);
    }
